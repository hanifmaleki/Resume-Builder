name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: sonar-scanner
      uses: Warchant/setup-sonar-scanner@v1
      with:
      # Sonar-scanner version
      version: # default is 4.2.0.1873
